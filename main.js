var sketch = 0
var randomNumber = Math.floor(Math.random()*quickDrawDataset)
console.log(randomNumber)
document.getElementById("esbocopedido").innerHTML = sketch
var timerCounter = 0
var timerCheck = []
var drawSketch = []
var answerHolder = []
var score = 0